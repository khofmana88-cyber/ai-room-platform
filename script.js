const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Close menu':'Open menu')});nav.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')})}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
const form=document.querySelector('#inquiry-form');if(form){const success=form.querySelector('.success');const validate=field=>{const error=field.parentElement.querySelector('.error');let message='';if(field.validity.valueMissing)message='Please complete this field.';else if(field.validity.typeMismatch)message='Enter a valid business email address.';else if(field.validity.tooShort)message=`Please provide at least ${field.minLength} characters.`;field.setAttribute('aria-invalid',String(Boolean(message)));error.textContent=message;return !message};form.querySelectorAll('input,select,textarea').forEach(field=>{field.addEventListener('blur',()=>validate(field));field.addEventListener('input',()=>{if(field.getAttribute('aria-invalid')==='true')validate(field)})});form.addEventListener('submit',event=>{event.preventDefault();const fields=[...form.querySelectorAll('input,select,textarea')];const valid=fields.map(validate).every(Boolean);if(!valid){fields.find(f=>f.getAttribute('aria-invalid')==='true')?.focus();return}success.classList.add('show');success.focus()});form.querySelector('.reset-form').addEventListener('click',()=>{success.classList.remove('show');form.querySelector('input')?.focus()})}
